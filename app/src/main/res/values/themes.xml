<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Islami" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_color</item>
        <item name="android:statusBarColor">@color/primary_color</item>
    </style>

    <style name="Theme.Islami" parent="Base.Theme.Islami" />
    <style name="bottomNavigation">
        <item name="android:background">@color/primary_color</item>
        <item name="itemIconSize">50dp</item>
        <item name="itemIconTint">@color/home_button_nav_selector</item>
        <item name="itemTextColor">@color/home_button_nav_selector</item>
    </style>
    <style name="background">
       <item name="android:background">@drawable/main_background</item>
    </style>
</resources>